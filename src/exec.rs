// use std::collections::BTreeMap;

// use crate::{expr::Expr, rule::Rule};


// pub fn rewrite(rules: Vec<&Rule>, state: Expr) -> Option<Expr> {
//     for Rule { pattern, expr } in rules {
//         if let Some(context) = matches(Context::new(), &pattern, &state) {

//         }
//     }
//     None
// }
